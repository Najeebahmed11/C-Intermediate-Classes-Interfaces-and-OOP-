using System;
using System.Collections.Generic;
using System.Linq;

namespace csharpadvanvce
{
    class Program
    {
        public static void Main(string[] args)
        {
           //Asynchronous Programming:
           //in sync prog programme is executed line by line one at a time
           //when func is called programme execution have to wait untill function return something
           //e.g when we access a database,a website
           //Async rog:a function is called programme execution to cintinues to next line without waiting for func to complete
           //we have to provide callback in that function
           //async improves responsess of your app
           //e.g,windows media player,web browser
           //use:working with files,databases,images
           //approch:multi-threading,callback,async/await
           //
        }
    }
}
